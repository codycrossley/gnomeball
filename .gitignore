# Build output
build/
out/

# Gradle internals
.gradle/

# IntelliJ
.idea/
*.iml

# macOS
.DS_Store
**/.DS_Store

# Local-only dev convenience (multi-instance test launchers) -- not for Plugin Hub submission
local-dev.gradle

# Kept locally for manual reconnect testing, but untracked so the Hub's build=standard clone never
# sees it -- it depends on the mockwebserver test dependency, which standard's replacement
# build.gradle template doesn't include.
src/test/java/gay/runescape/gnomeball/EventSocketTest.java
